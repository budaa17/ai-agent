node_modules/
dist/
.env
coverage/
*.log
data/reports/
# Demo artifacts are regenerated byte-for-byte differently on every seed run
# (PDFs embed a producer id), so they churn the diff for no benefit.
data/artifacts/tenant-demo/
backups/
.deployments/
.env.production
